# CLI Tool to get an JWT ID token from a Cognito Userpool via SRP 

## Installation/Build via Docker
```
docker build -t get_id_token .
```

## Run via Docker

```
docker run -it get_id_token --help 
```
